"""Representative crawler payloads as plain Python dicts.

These mirror the exact key paths the crawler parsers read, so the offline
unit tests exercise the real parsing branches without any network access.
Kept as Python (not JSON) to keep Hebrew strings readable and allow inline
comments documenting each shape variant.
"""
